export interface StudentPersonalData {
    firstName: string;
    lastName: string;
    email: string;
    phoneNumber: string;
}

export interface ProfessorPersonalData {
    firstName: string;
    lastName: string;
    email: string;
}